import { useEffect } from "react";
import { Earth } from "@/components/giscomponents";

const EarthPage = (props: any) => {
    useEffect(() => {}, []);

    return (
        <div>
            <Earth />
        </div>
    );
};

export default EarthPage;
